classDiagram
    class CreditCard {
        -number : string
        -cvc : string
        -pin : string
        #balance : float
        +owner_name : string
        
        +pay(amount: float, pin_code: string) string
        -check_pin(pin_code: string) boolean
        +get_balance() float
    }