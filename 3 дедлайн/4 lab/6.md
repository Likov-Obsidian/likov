classDiagram
    class User {
        - int user_id
        - String username
        - String email
        - List~Post~ posts
        + User(user_id, username, email)
        + create_post(String content) Post
        + delete_post(Post post) void
        + __str__() String
    }
    
    class Post {
        - int post_id
        - String content
        - User author
        - List~Comment~ comments
        - String created_at
        + Post(content, author)
        + add_comment(User user, String text) Comment
        + delete() void
        + show_post() void
        + __str__() String
    }
    
    class Comment {
        - int comment_id
        - User author
        - String text
        - Post post
        - String created_at
        + Comment(author, text, post)
        + __str__() String
    }
    
    User "1" o-- "*" Post : создает (Агрегация)
    Post "1" *-- "1" User : автор (Композиция)
    Post "1" *-- "*" Comment : содержит (Композиция)
    Comment "1" *-- "1" Post : относится к (Композиция)