classDiagram
    class Song {
        - String title
        - int duration
        + Song(title, duration)
        + __str__() String
    }
    
    class PlayList {
        - String name
        - List~Song~ songs
        + PlayList(name)
        + add_song(Song song) void
        + remove_song(String title) void
        + get_total_duration() int
        + show_playlist() void
    }
    
    PlayList "1" -- "*" Song : содержит