classDiagram
    class Product {
        - String name
        - float price
        + Product(name, price)
        + __str__() String
    }
    
    class Order {
        - String order_id
        - String status
        - List~Product~ products
        - DeliveryPerson delivery_person
        + Order(order_id, status)
        + add_product(Product product) void
        + set_delivery_person(DeliveryPerson person) void
        + get_total_price() float
        + __str__() String
    }
    
    class DeliveryPerson {
        - String name
        - int speed
        + DeliveryPerson(name, speed)
        + deliver(Order order) String
    }
    
    class Courier {
        - String phone_number
        + Courier(name, speed, phone_number)
        + call_client() String
        + deliver(Order order) String
    }
    
    class Drone {
        - int max_altitude
        + Drone(name, speed, max_altitude)
        + fly() String
        + deliver(Order order) String
    }
    
    Order "1" *-- "*" Product : содержит (Композиция)
    Order "1" o-- "1" DeliveryPerson : закреплен за (Агрегация)
    DeliveryPerson <|-- Courier
    DeliveryPerson <|-- Drone